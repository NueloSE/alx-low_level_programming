#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: Always 0 on success
 */

int main(void)
{
	int nums = 48;

	for (; nums < 58; nums++)
		putchar(nums);
	putchar('\n');
	return (0);
}
